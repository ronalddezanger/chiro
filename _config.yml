# Site settings
# These are used to personalize your new site. If you look in the HTML files,
# you will see them accessed via {{ site.title }}, {{ site.email }}, and so on.
# You can create any custom variable you would like, and they will be accessible
# in the templates via {{ site.myvariable }}.
title: Chiropractie van Leeuwen
email: info@chiropractievanleeuwen.nl
description: >- # this means to ignore newlines until "baseurl:"
  Welko bij Chiropractie van Leeuwen. Op deze website kunt u lezen wat chiropractie voor u kan betekenen, wie wij zijn en waar u ons kunt vinden.
baseurl: "" # the subpath of your site, e.g. /blog
url: "http://www.chiropractievanleeuwen.nl" # the base hostname & protocol for your site, e.g. http://example.com
# twitter_username: jekyllrb
# github_username:  jekyll

# Build settings
markdown: kramdown
# theme: minima
plugins:
  - jekyll-feed

# Exclude from processing.
# The following items will not be processed, by default. Create a custom list
# to override the default setting.
exclude:
  - Gemfile
  - Gemfile.lock
  - node_modules
  - vendor/bundle/
  - vendor/cache/
  - vendor/gems/
  - vendor/ruby/
  - node_modules
